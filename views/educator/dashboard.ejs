<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Educator Dashboard</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body class="bg-gray-50">
    <div class="min-h-screen p-6">
        <!-- Top Navigation Bar -->
        <div class="flex justify-between items-center mb-8">
            <h1 class="text-2xl font-bold text-gray-800">Educator Dashboard</h1>
            <a href="/signout" class="inline-flex items-center px-4 py-2 bg-red-600 text-white font-medium rounded-xl hover:bg-red-700 transition-all duration-200 shadow-md hover:shadow-lg hover:-translate-y-0.5">
                <i class="fas fa-sign-out-alt mr-2"></i>Sign Out
            </a>
        </div>

        <!-- Welcome Section -->
        <div class="bg-gradient-to-r from-indigo-600 to-indigo-800 rounded-2xl p-8 mb-8 shadow-lg">
            <h1 class="text-4xl font-bold text-white mb-2">Welcome, <%= name %>! 👋</h1>
            <p class="text-indigo-100 text-lg">Manage your courses and track student progress from your dashboard.</p>
        </div>

        <!-- Action Buttons -->
        <div class="flex flex-wrap gap-4 mb-8">
            <a href="/courses/create" class="inline-flex items-center px-6 py-3 bg-indigo-600 text-white font-semibold rounded-xl hover:bg-indigo-700 transition-all duration-200 shadow-md hover:shadow-lg hover:-translate-y-0.5">
                <i class="fas fa-plus me-2"></i>Create New Course
            </a>
            <button class="inline-flex items-center px-6 py-3 bg-white text-indigo-600 font-semibold rounded-xl hover:bg-gray-50 transition-all duration-200 shadow-md hover:shadow-lg hover:-translate-y-0.5 border border-indigo-200">
                <i class="fas fa-book me-2"></i>My Courses
            </button>
            <button class="inline-flex items-center px-6 py-3 bg-white text-indigo-600 font-semibold rounded-xl hover:bg-gray-50 transition-all duration-200 shadow-md hover:shadow-lg hover:-translate-y-0.5 border border-indigo-200">
                <i class="fas fa-chart-bar me-2"></i>View Reports
            </button>
        </div>

        <!-- Available Courses Section -->
        <div class="max-w-7xl mx-auto">
            <%- include('../courses/AvailableCourse.ejs') %>
        </div>
    </div>
</body>
</html>