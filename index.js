const app = require("./app");

app.listen(3000, () => {
  console.log("Started express server at port 3000");
});
// This code imports the Express application from app.js and starts a server listening on port 3000.